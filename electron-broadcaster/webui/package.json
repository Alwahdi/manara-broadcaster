{
  "name": "wiva-webui",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "WIVA Agent modern web UI (Viewer, Admin, Setup) — Arabic, RTL, offline-capable LAN app.",
  "scripts": {
    "dev": "next dev --hostname 0.0.0.0 --port 5273",
    "build": "tsc --noEmit && next build && node scripts/export-to-dist.mjs",
    "preview": "next dev --hostname 0.0.0.0 --port 5273",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fontsource/cairo": "^5.2.7",
    "@fontsource/tajawal": "^5.2.7",
    "@tanstack/react-query": "^5.101.2",
    "next": "^16.0.10",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "typescript": "^5.7.3"
  },
  "overrides": {
    "postcss": "^8.5.10"
  }
}
